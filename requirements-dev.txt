# requirements-dev.txt — tools for testing and code quality. Not needed in production.
#
# Install with:  pip install -r requirements-dev.txt
pytest==8.3.5
pytest-cov==6.1.0
ruff==0.11.2  # linter + formatter: catches unused imports, whitespace, style issues
